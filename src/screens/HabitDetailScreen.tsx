import React from 'react';
import { View, Text } from 'react-native';

export default function HabitDetailScreen() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Pantalla Detalle del Hábito</Text>
        </View>
    );
}
